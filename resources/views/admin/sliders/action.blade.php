<div class="d-flex items-center justify-content-center">
    <a href="{{ route('admin.sliders.edit', $id) }}" class="btn btn-primary btn-sm mr-2" title="Edit">
        <i class="fas fa-edit"></i>
    </a>
    <a href="{{ route('admin.sliders.destroy', $id) }}" class="btn btn-danger btn-sm delete_btn" title="Delete">
        <i class="fas fa-trash"></i>
    </a>
</div>
